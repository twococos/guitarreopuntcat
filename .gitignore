# Dependències
node_modules/
package-lock.json

# Base de dades
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
data/*.db*
backups/

# BD d'analítica (no entra a git per a no engreixar el repo)
data/analytics.db
data/analytics.db-shm
data/analytics.db-wal

# Base de dades GeoIP MaxMind (descarregada per separat)
data/GeoLite2-*.mmdb

# Variables d'entorn
.env
.env.local
# El .env del desplegament viu al servidor, mai al repo
# (deploy/.env.example sí que hi entra: és la plantilla).
deploy/.env

# Còpia de la BD preparada per pujar al servidor
canconer-prod.db

# Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Altres
.DS_Store
.vscode/
.idea/

# Claude
.claude/
